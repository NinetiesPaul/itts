{% extends 'base.html.twig' %}

{% block title %}Admin Home{% endblock %}

{% block javascripts %}
    <script>
    </script>
{% endblock %}

{% block navbar %}
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ url('admin_home') }}">ITTS</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Logged As <b>{{ app.user.name }}</b>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ url('admin_calls') }}">Calls</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ url('users') }}">Users</a></li>
                            <li><a class="dropdown-item" href="{{ url('departments') }}">Departments</a></li>
                            <li><a class="dropdown-item" href="{{ url('equipments') }} ">Equipments</a></li>
                            <li><a class="dropdown-item" href="{{ url('equipment_type') }} ">Equipments Type</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ url('app_logout') }}">Logout</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}

{% block body %}
    <div class="container border rounded" style="margin-top: 2em; min-height: 20em">
        <h2>IT TroubleShooting</h2>

        You have <b>{{ new_calls }}</b> on-going calls<br/>
        You have <b>{{ on_going_calls }}</b> new calls<br/>
        <br/>
        You're currently managing:
        <ul>
            <li><b>{{ equipments }}</b> equipments</li>
            <li><b>{{ users }}</b> users</li>
            <li><b>{{ departments }}</b> departments</li>
        </ul>
    </div>
{% endblock %}
